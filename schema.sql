
CREATE TABLE IF NOT EXISTS `fitbit_steps` (
    start STRING,
    end STRING,
    steps INTEGER
);

CREATE TABLE IF NOT EXISTS `fitbit_sleep` (
    start STRING,
    end STRING,
    state INTEGER
);


